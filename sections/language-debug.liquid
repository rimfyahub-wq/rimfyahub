{% comment %}
  Debug section to show language information
{% endcomment %}

<div style="position: fixed; bottom: 20px; right: 20px; background: rgba(0,0,0,0.9); color: white; padding: 20px; border-radius: 12px; border: 2px solid #00e5ff; z-index: 9999; max-width: 400px; font-size: 14px;">
  <h3 style="color: #00e5ff; margin: 0 0 10px 0;">🌐 Language Debug Info</h3>
  
  <p><strong>Current Language:</strong><br>
  {{ localization.language.endonym_name }} ({{ localization.language.iso_code }})</p>
  
  <p><strong>Available Languages ({{ localization.available_languages.size }}):</strong></p>
  <ul style="margin: 5px 0; padding-left: 20px;">
    {% for language in localization.available_languages %}
      <li>{{ language.endonym_name }} ({{ language.iso_code }}) 
        {% if language.iso_code == localization.language.iso_code %}✓{% endif %}
      </li>
    {% endfor %}
  </ul>
  
  <p><strong>Test Translation:</strong><br>
  {{ 'sections.hero.shop_now' | t }}</p>
  
  <p><strong>Current URL:</strong><br>
  {{ request.path }}</p>
  
  <p style="font-size: 12px; color: #888; margin-top: 10px;">
    If only 1 language shows, go to:<br>
    <strong>Shopify Admin → Settings → Languages</strong><br>
    and publish more languages.
  </p>
</div>

{% schema %}
{
  "name": "Language Debug",
  "settings": []
}
{% endschema %}
